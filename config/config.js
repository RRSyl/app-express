const config = {
    development: {
        username: "postgres",
        password: "postgres",
        database: "hw-8",
        host: "localhost",
        dialect: "postgres"
    }
}

module.exports = config;